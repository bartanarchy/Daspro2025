## THE ANSWER FROM CINEMA QUESTION
1. No, we do not need to fill the array starting from index 0. We can fill any index first, but the index that we do not fill will have the value null
2. There is a null because that index was never given a value
4. audience.length shows how many rows the 2d array has
   audience[0].length shows how many column are inside row 0
   Do audience[0].length, audience[1].length, audience[2].length, and audience[3].length have the same value?
   Yes. They are all 2 because the instantiation has a value [4][2] it means theres 4 rows and 2 columns
7. Advantages of foreach   : easier to write, easier to read, no need to worry about index numbers
   Disadvantages of foreach: we cannot access the index directly, we cannot skip or jump to specific index
8. i have 4 row if i turn into index it will be 0,1,2,3 so the max row index is 3
9. each row has 2 column if i tur into index it will be 0,1. so the max column index is 1
13. String.join() is used to join all elements in the array into one string, using the separator we choose.

## THE ANSWER FROM NUMBERS QUESTION
2. Arrays.toString() is used to convert an array into a string so we can print the whole array easily
3.The default value for int elements is 0
5. When you create the array, you must give the same column size for all rows for example like int[][] myNumbers = new int[3][5]; No After an array is created, the length cannot be change. If you need more space, you must create a new array

## THE ANSWER FROM SIAKAD QUESTION
1. With dynamic input, the array size can follow how many students and courses the user wants. The program can now work for any number of students and any number of courses.

